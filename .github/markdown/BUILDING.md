# Running the development build locally

`npm install`

Warning: **Wine is required if running on Linux**

`npm run dev`

# Building for Windows & Linux

`npm install`

`npm run build`

The output files are in the `dist` folder
